# 選ぶ選ばないの全パターン

```c++
// nつの要素に対して選ぶ選ばないの全パターンを試す
int _to = pow(2, n);
for (int i = 1; i < _to; i++) {
  rep(j, n) {
    if (i & (1 << j)) {
      // 選んだ
    } else {
      // 選ばなかった
    }
  }
}
```
